# For more information about this configuration visit
# https://docs.fastlane.tools/actions/scan/#scanfile

# In general, you can use the options available
# fastlane scan --help

workspace("MBCarKit.xcworkspace")
scheme("MBCarKitTests")

code_coverage(true)

derived_data_path("fastlane_builds/Tests")
output_directory("sonar_reports")
output_types("html,junit")

devices(["iPhone X"])
clean(true)
